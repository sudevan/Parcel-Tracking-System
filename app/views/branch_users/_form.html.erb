<%= bootstrap_form_with(model: branch_user, local: true) do |form| %>
  <% if branch_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(branch_user.errors.count, "error") %> prohibited this branch_user from being saved:</h2>

      <ul>
        <% branch_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.select(:email, options_for_select(@users  ), prompt: "Please Select" ) %>

   <%= form.select(:city, options_for_select(@servicelocations  ), prompt: "Please Select" ) %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
